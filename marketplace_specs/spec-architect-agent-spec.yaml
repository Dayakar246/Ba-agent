openapi: "3.0.0"
info:
  title: "Spec Architect Micro-Agent API"
  version: "1.0.0"
  description: "Independent REST endpoint for the BA Agent Pro Spec Architect node."
servers:
  - url: "https://ba-agent-aqd8c3d8dtdrbcat.centralus-01.azurewebsites.net"
paths:
  /api/agents/generate-trd:
    post:
      summary: "Spec Architect Agent"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                extraction:
                  type: object
                gaps:
                  type: object
                lob:
                  type: string
      responses:
        '200':
          description: "TRD Markdown string"
